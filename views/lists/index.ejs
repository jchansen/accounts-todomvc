<div class="container">
  <table class="table">
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th>
        <th>View</th>
      </tr>
    </thead>
    <tbody>
      <% _.each(tenants, function (tenant, index) { %>
        <tr>
          <td><%= index + 1 %></td>
          <td><%= tenant.name %></td>
          <td><a href="/lists/<%= tenant.name %>">view</a></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>